name: Deploy — production

on:
  push:
    branches: [main]

# Vercel deploys automatically via GitHub integration
# This file is a placeholder until Phase 2 backend is added
# No jobs needed here right now

jobs:
  placeholder:
    name: deploy-ready
    runs-on: ubuntu-latest
    steps:
      - name: Vercel handles frontend deployment automatically
        run: echo "Frontend deployed via Vercel GitHub integration"